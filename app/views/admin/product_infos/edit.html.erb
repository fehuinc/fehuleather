<h1><%= @info.name.titlecase %></h1>
<h4 class="center"><%= "Info for the #{@product.name}" %></h4>

<%= form_for [:admin, @info] do |f| %>
  <%= render "admin/product_infos/fields", f: f, edit: true %>
<% end %>
